<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunny.datapillar.workbench.module.workflow.mapper.JobComponentMapper">

    <select id="selectAllComponents" resultType="com.sunny.datapillar.workbench.module.workflow.entity.JobComponent">
        SELECT id, component_code, component_name, component_type,
               job_params, description, icon, color, sort_order
        FROM job_component
        WHERE status = 1 AND is_deleted = 0
        ORDER BY sort_order, id
    </select>

    <select id="selectByCode" resultType="com.sunny.datapillar.workbench.module.workflow.entity.JobComponent">
        SELECT id, component_code, component_name, component_type,
               job_params, description, icon, color, sort_order
        FROM job_component
        WHERE component_code = #{code} AND is_deleted = 0
    </select>

    <select id="selectByType" resultType="com.sunny.datapillar.workbench.module.workflow.entity.JobComponent">
        SELECT id, component_code, component_name, component_type,
               job_params, description, icon, color, sort_order
        FROM job_component
        WHERE component_type = #{type} AND status = 1 AND is_deleted = 0
        ORDER BY sort_order, id
    </select>

</mapper>
