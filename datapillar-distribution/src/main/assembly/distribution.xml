<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0
          http://maven.apache.org/xsd/assembly-2.2.0.xsd">
    <id>bin</id>
    <formats>
        <format>tar.gz</format>
    </formats>
    <includeBaseDirectory>true</includeBaseDirectory>

    <fileSets>
        <!-- bin Directory - startup script -->
        <fileSet>
            <directory>${project.basedir}/bin</directory>
            <outputDirectory>bin</outputDirectory>
            <fileMode>0755</fileMode>
            <includes>
                <include>*.sh</include>
            </includes>
        </fileSet>

        <!-- conf Directory - Configuration file -->
        <fileSet>
            <directory>${project.basedir}/conf</directory>
            <outputDirectory>conf</outputDirectory>
            <fileMode>0644</fileMode>
            <includes>
                <include>*</include>
            </includes>
        </fileSet>

        <!-- libs Directory - Depend on jar -->
        <fileSet>
            <directory>${project.build.directory}/datapillar-${project.version}/libs</directory>
            <outputDirectory>libs</outputDirectory>
            <fileMode>0644</fileMode>
            <includes>
                <include>*.jar</include>
            </includes>
        </fileSet>

        <!-- data Directory - Empty directory -->
        <fileSet>
            <directory>${project.build.directory}/datapillar-${project.version}/data</directory>
            <outputDirectory>data</outputDirectory>
            <excludes>
                <exclude>**/*</exclude>
            </excludes>
        </fileSet>

        <!-- logs Directory - Empty directory -->
        <fileSet>
            <directory>${project.build.directory}/datapillar-${project.version}/logs</directory>
            <outputDirectory>logs</outputDirectory>
            <excludes>
                <exclude>**/*</exclude>
            </excludes>
        </fileSet>
    </fileSets>
</assembly>
