<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunny.datapillar.admin.module.user.mapper.UserMapper">

    <select id="findByUsernameWithRoles" resultType="com.sunny.datapillar.admin.module.user.entity.User">
        SELECT u.*
        FROM users u
        WHERE u.username = #{username} AND u.deleted = 0
    </select>

    <select id="findByUsername" resultType="com.sunny.datapillar.admin.module.user.entity.User">
        SELECT * FROM users WHERE username = #{username} AND deleted = 0
    </select>

    <select id="getUserRoleCodes" resultType="string">
        SELECT DISTINCT r.type
        FROM roles r
        INNER JOIN user_roles ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
    </select>

    <select id="getUserPermissionCodes" resultType="string">
        SELECT DISTINCT p.code
        FROM permissions p
        WHERE p.id IN (
            SELECT rp.permission_id
            FROM role_permissions rp
            INNER JOIN user_roles ur ON rp.role_id = ur.role_id
            WHERE ur.user_id = #{userId}
            UNION
            SELECT up.permission_id
            FROM user_permission_overrides up
            WHERE up.user_id = #{userId}
        )
    </select>

    <delete id="deleteUserRoles">
        DELETE FROM user_roles WHERE user_id = #{userId}
    </delete>

    <insert id="insertUserRole">
        INSERT INTO user_roles (user_id, role_id) VALUES (#{userId}, #{roleId})
    </insert>

    <delete id="deleteUserPermissions">
        DELETE FROM user_permission_overrides WHERE user_id = #{userId}
    </delete>

    <insert id="insertUserPermission">
        INSERT INTO user_permission_overrides (user_id, object_id, permission_id)
        VALUES (#{userId}, #{objectId}, #{permissionId})
    </insert>

    <select id="selectByIdAndTokenSign" resultType="com.sunny.datapillar.admin.module.user.entity.User">
        SELECT * FROM users WHERE id = #{userId} AND token_sign = #{tokenSign} AND deleted = 0
    </select>

</mapper>
