<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunny.datapillar.studio.module.workflow.mapper.JobDependencyMapper">

    <resultMap id="DependencyResponseResult" type="com.sunny.datapillar.studio.dto.workflow.response.JobDependencyResponse">
        <result property="id" column="id"/>
        <result property="workflowId" column="workflow_id"/>
        <result property="jobId" column="job_id"/>
        <result property="parentJobId" column="parent_job_id"/>
    </resultMap>

    <select id="selectByWorkflowId" resultMap="DependencyResponseResult">
        SELECT id, workflow_id, job_id, parent_job_id
        FROM job_dependency
        WHERE workflow_id = #{workflowId} AND is_deleted = 0
    </select>

    <select id="selectByJobId" resultMap="DependencyResponseResult">
        SELECT id, workflow_id, job_id, parent_job_id
        FROM job_dependency
        WHERE job_id = #{jobId} AND is_deleted = 0
    </select>

    <update id="deleteDependency">
        UPDATE job_dependency SET is_deleted = 1
        WHERE workflow_id = #{workflowId}
          AND job_id = #{jobId}
          AND parent_job_id = #{parentJobId}
          AND is_deleted = 0
    </update>

    <update id="deleteByWorkflowId">
        UPDATE job_dependency SET is_deleted = 1
        WHERE workflow_id = #{workflowId} AND is_deleted = 0
    </update>

    <update id="deleteByJobId">
        UPDATE job_dependency SET is_deleted = 1
        WHERE (job_id = #{jobId} OR parent_job_id = #{jobId}) AND is_deleted = 0
    </update>

    <select id="existsDependency" resultType="int">
        SELECT COUNT(*) FROM job_dependency
        WHERE job_id = #{jobId} AND parent_job_id = #{parentJobId} AND is_deleted = 0
    </select>

</mapper>
