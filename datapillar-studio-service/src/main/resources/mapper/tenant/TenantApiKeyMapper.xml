<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunny.datapillar.studio.module.tenant.mapper.TenantApiKeyMapper">

    <select id="selectByTenantId" resultType="com.sunny.datapillar.studio.module.tenant.entity.TenantApiKey">
        SELECT *
        FROM tenant_api_keys
        WHERE tenant_id = #{tenantId}
        ORDER BY created_at DESC, id DESC
    </select>

    <select id="selectByTenantIdAndName" resultType="com.sunny.datapillar.studio.module.tenant.entity.TenantApiKey">
        SELECT *
        FROM tenant_api_keys
        WHERE tenant_id = #{tenantId}
          AND name = #{name}
        LIMIT 1
    </select>

    <select id="countUsableByTenantId" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM tenant_api_keys
        WHERE tenant_id = #{tenantId}
          AND status = 1
          AND (expires_at IS NULL OR expires_at > #{now})
    </select>
</mapper>
