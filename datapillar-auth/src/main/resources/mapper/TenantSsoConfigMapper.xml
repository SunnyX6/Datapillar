<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunny.datapillar.auth.mapper.TenantSsoConfigMapper">

    <resultMap id="TenantSsoConfigResult" type="com.sunny.datapillar.auth.entity.TenantSsoConfig">
        <result property="id" column="id"/>
        <result property="tenantId" column="tenant_id"/>
        <result property="provider" column="provider"/>
        <result property="status" column="status"/>
        <result property="baseUrl" column="base_url"/>
        <result property="configJson" column="config_json"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectByTenantIdAndProvider" resultMap="TenantSsoConfigResult">
        SELECT id, tenant_id, provider, status, base_url, config_json, created_at, updated_at
        FROM tenant_sso_configs
        WHERE tenant_id = #{tenantId}
          AND provider = #{provider}
        LIMIT 1
    </select>

</mapper>
